<!DOCTYPE html>
<html>
  <head>
    <title>ChooseForMe</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <main class='bg-gradient-to-t from-slate-500 to-slate-400 min-h-screen'>
    <% if notice.present? %>
    <div class='fixed flex items-start bottom-0 justify-center'>
      <div class='bottom-2 card'>
        <%= notice %>
      </div>
    </div>
    <% end %>
      <%= yield %>
      <div class='card bg-red-200'>
        <%= debug(params) if Rails.env.development? %>
      </div>
    </main>
  </body>
</html>
